ingress:
  tls: false
  annotations:
    cert-manager.io/cluster-issuer: cert-issuer-letsencrypt-dns

  
config:
  siteName: "Rubin Science Platform @ lapp in2p3"

pull-secret:
  enabled: true
  path: secret/k8s_operator/rsp-lapp/pull-secret
